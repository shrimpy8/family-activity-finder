/**
 * Re-export shared types from the single source of truth
 * This maintains backward compatibility while using shared definitions
 */
export type { TimeSlot, ActivityFormData, Recommendation, RecommendResponse, LLMProvider, OutputFormat } from '../shared/types';
